#Escreva um algoritmo que mostra os
#Numeros 1 a 20, impar.


import streamlit as st
import time

st.title("Atividade 1")

st.header("Contagem de 1 a 20.")

if st.button("Iniciar"):
    for i in range(1,20 +1 ,2) :
        st.success(i)
        time.sleep(1)
    st.header("FIM")
